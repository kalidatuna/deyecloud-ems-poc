Hi, I checked the current DeyeCloud integration before replying. It already covers official token auth, station/device discovery, `/station/latest` telemetry, and one live `order/...` control path, so I built a standalone Python diagnostic proof around that flow instead of guessing from scratch: https://github.com/kalidatuna/deyecloud-ems-poc

For a first fixed milestone I can do authentication + discovery + telemetry + your exact Dynamic Control read/write validation + command-result reporting for **$60**, with writes dry-run by default until we agree the test inverter and payload from your spec. I’m 17 and based in Georgia, so before paid work starts I’d need you to confirm that is okay and that you can pay by international bank transfer to Georgia or another direct route that can legally pay me.
